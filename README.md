## octavo-bn-scraper

This is a script to get the BN articles for text reuse detection from Octavo API.

Intended to be run on the same server (Pouta) as the Octavo API, and to access it by localhost.
